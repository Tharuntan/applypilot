// Used automatically by `ng serve` / `npm start` (development build).
// Local backend runs on :8090 in this environment (8080 is taken by another app).
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8090/api',
};
